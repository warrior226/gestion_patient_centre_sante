apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rdvdb-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: rdvdb-deployment
  labels:
    app: rdvdb
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rdvdb
  template:
    metadata:
      labels:
        app: rdvdb
    spec:
      containers:
        - name: rdvdb
          image: mysql:latest
          ports:
            - containerPort: 3306
          env:
            - name: RDV_MYSQL_DATABASE
              valueFrom:
                configMapKeyRef:
                  name: rayaisse-configmap
                  key: RDV_MYSQL_DATABASE
            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: mysql-secret
                  key: root-password
          volumeMounts:
            - name: mysql-storage
              mountPath: /var/lib/mysql
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
      volumes:
        - name: mysql-storage
          persistentVolumeClaim:
            claimName: rdvdb-pvc

---
apiVersion: v1
kind: Secret
metadata:
  name: mysql-secret
type: Opaque
stringData:
  root-password: "root"

---
apiVersion: v1
kind: Service
metadata:
  name: rdvdb
spec:
  selector:
    app: rdvdb
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 3306
      targetPort: 3306