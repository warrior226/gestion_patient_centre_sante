apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: examendb-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: examendb-deployment
  labels:
    app: examendb
spec:
  replicas: 1
  selector:
    matchLabels:
      app: examendb
  template:
    metadata:
      labels:
        app: examendb
    spec:
      containers:
        - name: examendb
          image: mysql:latest
          ports:
            - containerPort: 3306
          env:
            - name: EXAMEN_MYSQL_DATABASE
              valueFrom:
                configMapKeyRef:
                  name: rayaisse-configmap
                  key: EXAMEN_MYSQL_DATABASE
            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: mysql-secret
                  key: root-password
          volumeMounts:
            - name: mysql-storage
              mountPath: /var/lib/mysql
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
      volumes:
        - name: mysql-storage
          persistentVolumeClaim:
            claimName: examendb-pvc

---
apiVersion: v1
kind: Secret
metadata:
  name: mysql-secret
type: Opaque
stringData:
  root-password: "root"

---
apiVersion: v1
kind: Service
metadata:
  name: examendb
spec:
  selector:
    app: examendb
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 3306
      targetPort: 3306