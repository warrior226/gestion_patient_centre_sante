apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: constantedb-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: constantedb-deployment
  labels:
    app: constantedb
spec:
  replicas: 1
  selector:
    matchLabels:
      app: constantedb
  template:
    metadata:
      labels:
        app: constantedb
    spec:
      containers:
        - name: constantedb
          image: mysql:latest
          ports:
            - containerPort: 3306
          env:
            - name: CONSTANTE_MYSQL_DATABASE
              valueFrom:
                configMapKeyRef:
                  name: rayaisse-configmap
                  key: CONSTANTE_MYSQL_DATABASE
            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: mysql-secret
                  key: root-password
          volumeMounts:
            - name: mysql-storage
              mountPath: /var/lib/mysql
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
      volumes:
        - name: mysql-storage
          persistentVolumeClaim:
            claimName: constantedb-pvc

---
apiVersion: v1
kind: Secret
metadata:
  name: mysql-secret
type: Opaque
stringData:
  root-password: "root"

---
apiVersion: v1
kind: Service
metadata:
  name: constantedb
spec:
  selector:
    app: constantedb
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 3306
      targetPort: 3306